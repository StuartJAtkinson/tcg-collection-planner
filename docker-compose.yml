services:
  postgres:
    image: postgres:17-alpine
    container_name: card-collection-db
    environment:
      POSTGRES_USER: cards
      POSTGRES_PASSWORD: cards
      POSTGRES_DB: cards
    ports:
      - "5254:5432" # host port avoids map-merch's postgres on 5432 (see H:\GitHub\PORTS.md)
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cards"]
      interval: 2s
      timeout: 2s
      retries: 30

volumes:
  pgdata:
