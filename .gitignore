node_modules/
data/
.env
*.csv
.playwright-mcp/
startStop.md
.claude/
.atelier-dev.log
.scratch/
cards.json.gz
ref/
*.mp4

# fetched booster photographs — regenerate with: node gen-packs.mjs
packs/

# fetched card art — regenerate with: node gen-art.mjs. 34 MB for the anatomy
# sample page, 8.1 GB for the whole catalogue at art_crop, 132 GB at png.
art/

# screenshots dropped while working -- at the repo root, and in screenshots/
# once they get tidied. No .png has ever been tracked. The 39 that had
# accumulated (config-schema-v2..v15 and friends, three of them byte-identical
# duplicates) were deleted on 2026-09-08: nothing referenced them and no doc
# embedded one. The rules stay so the next batch also stays out of history.
/*.png
/screenshots/

# generated ballot — regenerated by the look-at-it pass, never committed
STYLE.html

# a dump of browser state taken while debugging - personal, never committed
.localstorage-snapshot.json
