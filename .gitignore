node_modules/
data/
.env
*.csv
.playwright-mcp/
startStop.md
.claude/
.atelier-dev.log
.scratch/
cards.json.gz
ref/
*.mp4

# fetched booster photographs — regenerate with: node gen-packs.mjs
packs/

# fetched card art — regenerate with: node gen-art.mjs. 34 MB for the anatomy
# sample page, 8.1 GB for the whole catalogue at art_crop, 132 GB at png.
art/

# screenshots dropped while working -- at the repo root, and in screenshots/
# once they get tidied. No .png is tracked anywhere today.
/*.png
# ...and the same shots once they get tidied into a folder. 30 of them
# (config-schema-v2..v15 and friends) are iteration snapshots nothing
# references. Left on disk, out of history.
/screenshots/

# generated ballot — regenerated by the look-at-it pass, never committed
STYLE.html
