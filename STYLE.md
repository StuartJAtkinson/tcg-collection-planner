# Style — Card Collector v2

Visual questions. Each one needs someone to look at the rendered options; the
worksheet (`STYLE.html`) draws them.

- The one header bar carries two different "you are here" treatments: Printings/Binders/Decks/Search get a 2px `#10b981` underline, while Import and the cog get a filled `#10b981` pill with a ring. Unify them, or keep the split between the four routes and the two utilities?
